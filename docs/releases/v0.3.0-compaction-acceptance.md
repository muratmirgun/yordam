# Yordam v0.3.0 Context Compaction Acceptance

## Evidence provenance

This document is initially committed with the Task 8 source and deliberately
does not claim that its release gate has run. After the clean source commit is
verified, this file is updated in a child evidence-report commit. The evidence
report commit is not part of the tested source.

## Acceptance scope

The deterministic, credential-free acceptance entry point is:

```sh
go test ./internal/acceptance -run 'TestV030Compaction' -count=1
```

It covers manual success and replay; automatic threshold, disabled, and unknown
window policy; prior-summary reconstruction and preserved suffix; bounded large
tool references; provider refusal; context-too-large without normal-turn retry;
cancellation; evidence failure; known non-commit; uncertain commit; restart
reconstruction; immutable source-prefix hashing; and secret absence from summary
requests, evidence, durable/application events, TUI output, logs, and public
errors.

## User-facing behavior

Manual `/compact` requires an idle session and writes a native durable compaction
event bound to summary evidence. Automatic compaction uses the same summary
contract after a known-window reserve threshold is reached; it remains
unavailable when disabled or when the window is unknown. A context-too-large
provider failure exposes `/compact` and does not receive a normal provider retry.

Compaction never rewrites the journal. Context reconstruction verifies the
summary-evidence binding and retains the newer uncompacted suffix. A summary is
derived context, never original evidence. Cancellation gets a durable terminal
result. Known non-commits terminalize safely; an unprovable final commit remains
uncertain until recovery can resolve it.

## Security limits

The acceptance fixtures seed provider-key and sentinel values and require their
absence from provider summary input after admission, stored evidence, events,
TUI-visible output, logs, and public errors. This redaction boundary reduces
accidental disclosure but is not a sandbox and does not make arbitrary model or
user content safe to disclose.

## Exact verification commands

The following commands are required before this document is updated with a PASS
record, tested source commit, host toolchain, fixture digest, and wall times:

```sh
go test ./internal/compaction ./internal/context ./internal/orchestrator ./internal/app ./internal/tui/... ./internal/acceptance -count=1
go test ./... -count=1
go test -race ./... -count=1
go vet ./...
git diff --check
```

This initial source document makes no portability claim. Host execution and
portability coverage are recorded only from commands that actually ran.
