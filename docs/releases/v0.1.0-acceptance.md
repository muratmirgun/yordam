# Yordam v0.1.0 Acceptance

## Historical v0.1 release-candidate evidence (2026-07-15)

- Exact release-candidate source commit under test: `7b521381358010a511f2e126d3bc1b8baf126d2d`, on top of prior fix-wave evidence commit `efa96c2b7d9c5ffb008cb398ba92a3698cc7b22c`
- Historical evidence lineage: this document was committed after the tested source commit; its exact recording commit is reported in `.superpowers/sdd/post-fix-report.md` because embedding a commit's own hash would be self-referential
- Go version: `go version go1.26.4 darwin/arm64`
- Date: `2026-07-15`
- Snapshot version: `0.0.0-SNAPSHOT-none`; embedded date: `2026-07-15T20:54:13Z`
- macOS runner: PASS on local macOS 27.0 arm64; Darwin arm64 native and Darwin amd64 Rosetta archive smokes PASS
- Linux runner: PASS on Docker Desktop 29.6.1, Linux `aarch64`; Linux arm64 native-engine and Linux amd64 emulated archive smokes PASS
- Darwin amd64 archive SHA-256: `e3b3c02da1cc6bf58b50b15b7cc32b6904ac7ceb50f9c186ea9e232709a78c18`
- Darwin arm64 archive SHA-256: `6883b71b285112beecd474c121590e0ac5e305076204315e9f461efa75bd1c4f`
- Linux amd64 archive SHA-256: `319d87ed7aba15be4cb154848aded4d87a175db5ba72307918c0f7a004effa9c`
- Linux arm64 archive SHA-256: `2c63428a6a9d5a1e84498fbab838f2a4f071a4410bb2cadc556ca244cb2f844a`
- Darwin amd64 SBOM SHA-256: `736cb9545a8eac265ed5289d66eaa6d7854e292e395ae4e7ae6a5c1a667803a4`
- Darwin arm64 SBOM SHA-256: `bb08e6f61e4200629949b5198410b072fb275bd4e70d8c731acc25af1c8bdfcd`
- Linux amd64 SBOM SHA-256: `7e4e5c0868a1f3a8c4736015767b41ff2ca6f3317f7019b4bff62ff606d22d39`
- Linux arm64 SBOM SHA-256: `6e964f5237abbf740d7acfaae60ce4297c735802c6e4a2fc62e6378c242615f0`
- Snapshot checksum-file SHA-256: `1bb87841664cdb94f8c86e4238518f54ae4e66feb9611393f841a8a6227b2c08`

The complete local release-candidate run used `go test -tags acceptance ./internal/acceptance -run TestV010Acceptance -count=1 -v` after `goreleaser release --snapshot --clean` and reported all fourteen subtests as PASS. The same acceptance command with `-race` also passed.

### Historical native release smoke results

| Target | Execution | Architecture check | Version output | Result |
|---|---|---|---|---|
| Darwin arm64 | `./scripts/smoke-release.sh dist/yordam_0.0.0-SNAPSHOT-none_darwin_arm64.tar.gz` | host `uname -m`: `arm64`; binary: Mach-O 64-bit arm64 | `yordam 0.0.0-SNAPSHOT-none (none, 2026-07-15T20:54:13Z)` | PASS |
| Darwin amd64 | `arch -x86_64 ./scripts/smoke-release.sh dist/yordam_0.0.0-SNAPSHOT-none_darwin_amd64.tar.gz` | Rosetta `uname -m`: `x86_64`; binary: Mach-O 64-bit x86_64 | `yordam 0.0.0-SNAPSHOT-none (none, 2026-07-15T20:54:13Z)` | PASS |
| Linux arm64 | `docker run --rm --platform linux/arm64 ... ubuntu:24.04` with the repository mounted read-only | container `uname -m`: `aarch64` | `yordam 0.0.0-SNAPSHOT-none (none, 2026-07-15T20:54:13Z)` | PASS |
| Linux amd64 | `docker run --rm --platform linux/amd64 ... ubuntu:24.04` with the repository mounted read-only | container `uname -m`: `x86_64` | `yordam 0.0.0-SNAPSHOT-none (none, 2026-07-15T20:54:13Z)` | PASS |

## Current JSONC harness evidence (2026-07-17)

- Exact source commit under test: `2d371e77ceb30b354248231093c2622746425743`
- Provenance boundary: the source commit above contains only the test/security fixes; this evidence document is committed afterward so it cannot be mistaken for part of the tested source tree
- Go version: `go version go1.26.4 darwin/arm64`
- Local runner: `Darwin 27.0.0 arm64`
- Snapshot prerequisite: `goreleaser release --snapshot --clean` — PASS; snapshot version `0.0.0-SNAPSHOT-2d371e7`
- PTY harness: `go test ./internal/ptytest -count=1` — PASS (`7.351s`)
- Regular acceptance: `go test -tags acceptance ./internal/acceptance -count=1` — PASS (`5.108s`)
- Race acceptance: `go test -race -tags acceptance ./internal/acceptance -count=1` — PASS (`6.470s`)
- Full regular suite: `go test ./...` — PASS (`internal/ptytest` `7.047s`)
- Full race suite: `go test -race ./...` — PASS (`internal/ptytest` `8.702s`)
- Static analysis: `go vet ./...` and `go vet -tags=acceptance ./internal/acceptance` — PASS

The current acceptance harness uses only `~/.config/yordam/config.jsonc`. First launch creates an environment-name-only JSONC template naming `OPENAI_API_KEY`, scrubs inherited provider credentials, and opens the normal conversation TUI. Edits are applied with `/reload` without changing session or data identity; a newly named but missing credential applies the new state, blocks provider requests, emits restart guidance, and restores the submitted draft. Legacy TOML configuration files are ignored without automatic migration.

The regular and race acceptance commands above each ran the complete fourteen-subtest catalog below against the exact source commit and passed. Historical release hashes and cross-platform smoke results remain in the preceding section and are not artifacts of the current source commit.

## Current acceptance catalog for `2d371e77ceb30b354248231093c2622746425743`

### 1. Single binary startup
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/single_binary_startup' -count=1 -v`
Expected: one binary built in a fresh directory prints its version, opens the normal TUI, creates the environment-name-only `~/.config/yordam/config.jsonc` template with user-only permissions, creates a session, exits, and restores the terminal without persisting the sentinel credential value.
Result: PASS

### 2. Secret-free config reload
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/secret_free_config_reload' -count=1 -v`
Expected: first run opens the normal TUI, an atomically edited JSONC file becomes active through `/reload` in the same session, a real authenticated local-provider turn completes, and the sentinel credential value remains absent from the config, session data, artifacts, debug log, and PTY output.
Result: PASS

### 3. Scripted four-tool turn
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/scripted_four_tool_turn' -count=1 -v`
Expected: real read, search, edit, and shell calls complete in order and the final assistant text is durable.
Result: PASS

### 4. Ask allow and deny
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/ask_allow_and_deny' -count=1 -v`
Expected: ask mode applies one allowed edit, denies the shell before execution, leaves no shell side effect, and persists both decisions.
Result: PASS

### 5. Safe blocks mutations
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/safe_blocks_mutations' -count=1 -v`
Expected: safe mode permits an in-workspace read and denies edit and shell requests before either mutation executes.
Result: PASS

### 6. Auto scope and shell acknowledgement
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/auto_scope_and_shell_ack' -count=1 -v`
Expected: auto mode executes an inside edit, asks for outside read and unacknowledged shell access, then executes shell after durable acknowledgement.
Result: PASS

### 7. Exact diff and stale preimage
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/exact_diff_and_stale_preimage' -count=1 -v`
Expected: the edit preview contains the exact unified diff and a changed preimage prevents the prepared write.
Result: PASS

### 8. Cancel kills process group
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/cancel_kills_process_group' -count=1 -v`
Expected: cancelling `sleep 30 & wait` terminates the process group within three seconds and returns a cancelled result.
Result: PASS

### 9. Continue reconstructs session
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/continue_reconstructs_session' -count=1 -v`
Expected: bootstrap with `--continue` restores the same canonical workspace, session, model, mode, and durable transcript.
Result: PASS

### 10. Truncated tail recovery
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/truncated_tail_recovery' -count=1 -v`
Expected: an incomplete final fragment is preserved as an artifact and an unmatched tool start yields exactly one interruption without executing the tool.
Result: PASS

### 11. Model change next turn
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/model_change_next_turn' -count=1 -v`
Expected: a model change queued during a blocked turn leaves the first request on the old selection and applies the new profile and model to the second request.
Result: PASS

### 12. Git and non-Git workspaces
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/git_and_non_git' -count=1 -v`
Expected: shell mutations report Git status in a repository and render the exact non-Git filesystem-detection limitation in an ordinary directory.
Result: PASS

### 13. Four release targets
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/four_release_targets' -count=1 -v`
Expected: the snapshot has exactly Darwin/Linux by amd64/arm64 archives, four matching SPDX SBOMs, and eight verified checksum entries.
Result: PASS

### 14. Secret hygiene
Command: `go test -tags acceptance ./internal/acceptance -run 'TestV010Acceptance/secret_hygiene' -count=1 -v`
Expected: the sentinel is absent from auth/error/shell/session/artifact/debug/PTY generated outputs and provider key variables are absent from shell `env` output.
Result: PASS
